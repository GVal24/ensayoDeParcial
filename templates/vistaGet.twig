<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Lista de Videojuegos</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-collection-play me-2"></i>Lista de Videojuegos</h2>
        <a href="/videogames/new" class="btn btn-success">
            <i class="bi bi-plus-circle me-1"></i>Agregar nuevo videojuego
        </a>
    </div>

    <div class="table-responsive shadow rounded">
        <table class="table table-hover table-bordered align-middle mb-0 bg-white">
            <thead class="table-primary text-center">
                <tr>
                    <th>ID</th>
                    <th>Título</th>
                    <th>Año</th>
                    <th>Desarrolladora</th>
                    <th>Tipo</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                {% for game in games %}
                    <tr>
                        <td class="text-center">{{ game.id }}</td>
                        <td>{{ game.title }}</td>
                        <td class="text-center">{{ game.release_year }}</td>
                        <td>{{ game.developer }}</td>
                        <td>{{ game.type_id }}</td>
                        <td class="text-center">
                            <a href="/videogames/{{ game.id }}/edit" class="btn btn-sm btn-outline-primary me-1">
                                <i class="bi bi-pencil-square"></i>
                            </a>
                            <form action="/videogames/{{ game.id }}/delete" method="POST" style="display:inline;">
                            <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('¿Estás seguro de que querés eliminar este juego?');">
                                <i class="bi bi-trash"></i>
                            </button>
                            </form>
                            
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="6" class="text-center text-muted">No hay videojuegos cargados.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>

<!-- Bootstrap JS Bundle -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
